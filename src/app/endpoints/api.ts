import { environment } from 'src/environments/environment';

export const apiUrl = {

// Auth APIs 
  signUp: `${environment.baseUrl}signup API URL here`,
  signIn: `${environment.baseUrl}sign API URL here`,

//Common APIs

};
